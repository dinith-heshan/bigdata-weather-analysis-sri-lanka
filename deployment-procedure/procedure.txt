docker exec -it namenode /bin/bash
hdfs dfs -mkdir -p /data/raw

docker cp data/raw/locationData.csv namenode:/data/raw/locationData.csv
docker cp data/raw/weatherData.csv namenode:/data/raw/weatherData.csv

hdfs dfs -put /data/raw/locationData.csv /data/raw/
hdfs dfs -put /data/raw/weatherData.csv /data/raw/

hdfs dfs -ls /data/raw

cd ~/bigdata-weather-analysis-sri-lanka/preprocessing/validate-weather-data

mvn archetype:generate \
    -DgroupId=validate.weather \
    -DartifactId=preprocessing \
    -DarchetypeArtifactId=maven-archetype-quickstart \
    -DinteractiveMode=false


maven clean install

